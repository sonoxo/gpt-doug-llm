[project]
name = "zyra-defense-ontology"
version = "0.1.0"
description = "Simulation-only defense digital-twin ontology reference implementation"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["zyra_ontology"]
