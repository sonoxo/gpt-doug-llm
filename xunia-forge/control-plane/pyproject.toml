[project]
name = "xunia-forge-control"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "httpx>=0.27,<1", "uvicorn>=0.30,<1"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.uv]
package = false
