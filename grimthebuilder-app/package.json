{
  "name": "grimthebuilder",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "description": "Local XUNIA workspace/self-hosting integration runtime for the canonical GrimTheBuilder browser IDE",
  "scripts": {
    "start": "node server.mjs",
    "start:full": "node scripts/start-with-gpt-doug.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check scripts/start-with-gpt-doug.mjs && node --check lib/store.mjs && node --check lib/runtime.mjs && node --check public/bootstrap.js && node --check public/app.js && node --check public/gpt-doug-connector.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ws": "^8.18.0" }
}
