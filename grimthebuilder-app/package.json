{
  "name": "grimthebuilder",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "description": "Local XUNIA workspace/self-hosting integration runtime for the canonical GrimTheBuilder browser IDE",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib/store.mjs && node --check lib/runtime.mjs && node --check public/bootstrap.js && node --check public/app.js && node --check public/gpt-doug-connector.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ws": "^8.18.0" }
}
