{
  "name": "grimthebuilder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "XUNIA full-stack browser IDE and isolated project runtime",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.mjs && node --check lib/store.mjs && node --check lib/runtime.mjs && node --check public/app.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": { "ws": "^8.18.0" }
}
